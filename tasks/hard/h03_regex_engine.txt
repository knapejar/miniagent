GOAL: implement a small regular expression engine and make a test suite pass.

Create myre.py. You MUST NOT import the re module anywhere.

Implement match(pattern, text) -> bool (full match of the whole text) and
search(pattern, text) -> (start, end) or None (leftmost match).
Supported syntax:
- literal characters, and . matching any single character
- quantifiers * + ? applied to the previous single element or group
- character classes [abc], ranges [a-z0-9], negation [^abc]
- anchors ^ and $
- groups ( ) with alternation | inside and at top level
- escaping with backslash: \. \* \[ \\ and \d \w \s
Use recursive backtracking. Raise ReError(ValueError) for malformed patterns
such as '(' , '[a', '*abc', 'a**'.

Create test_myre.py with AT LEAST 70 asserts covering:
- literals and .: match("abc","abc") is True, match("a.c","abc") is True,
  match("a.c","ac") is False
- star/plus/question: "a*b" against "b","ab","aaab"; "a+b" against "b" is False;
  "colou?r" against both spellings
- classes: "[abc]+" , "[a-z]+[0-9]*" , "[^0-9]+" with positive and negative cases
- anchors: search("^ab","xab") is None, search("ab$","abx") is None
- groups and alternation: "(ab)+c", "(cat|dog)s?", "((a|b)c)+"
- escapes: "\d+" on "12345", "\w+" , "a\.b" matching "a.b" but not "axb"
- search returning correct (start,end) for at least 10 cases
- greedy behaviour: search("a+","caaab") == (1,4)
- every malformed pattern above raising ReError
The last line of the test file must print ALL OK.

FIRST STEP: call the plan tool with a short checklist of the features listed
above, then do NOT plan the whole implementation up front. Immediately create a
minimal version of the main file that handles only the FIRST item in the list above,
plus a test file with 3 asserts for it, and run it. Then add ONE feature at a time,
running the tests after every change. Think briefly between steps.

RULES
- Work in SMALL steps: implement ONE function or ONE group of tests at a time,
  then immediately run `python test_myre.py` and fix what fails.
- After EVERY change run the WHOLE test file, not just the test you just added.
  If something that passed before now fails, your last edit caused it: undo that
  edit before you add anything new.
- Do NOT restructure code that already works. Add new handling alongside it.
- Whenever a feature starts passing, record it in the plan as DONE so you know not
  to touch it again. The plan survives context cropping; the conversation does not.
- Use the edit tool for fixes. NEVER rewrite a whole file to change a few lines.
- Do not weaken or delete an assert to make the suite pass. Fix the code instead.
- Re-read the file with the read tool if you are unsure what is currently in it.
- DONE only when `python test_myre.py` prints ALL OK as its last line.
Then reply with a short summary of what you built and which bugs you had to fix.
